# AI解説文生成機能 要件定義

## 背景・目的

既存のランキングデータ変換パイプライン(S3 → NameIdentifier → JSON → GZIP → FTP)に、AIによる戦況解説文(自然文のコメンタリー)を追加する。

## 対象データ

`Race` → `Subrace`(複数) → `RankSnapshot`(時系列) → `RankItem`(rank / point / name / id)、および `Subrace` 単位の `DisplayName` リスト(id → 名前、匿名フラグ)。

レースごとに性質が異なる点に留意する。

| タイプ | 該当レース | 特徴 |
|---|---|---|
| じっくり系 | slimerace, fishing, casinoraid | snapshotLengthLimit=1000、長期間の推移を追える |
| 短期決戦系 | daifugo, daifugom, battle_pencil | snapshotLengthLimit=70、短時間で決着 |
| 多サブレース | casinoraid(4種目)、trinity(6サブレース) | サブレース間の比較が可能 |

## 確定した要件

- **出力粒度**: サブレースごとに1本の解説文を生成する
- **生成タイミング**: 既存パイプラインの実行都度(S3イベントごと)、全文を再生成する
- **出力先**: 既存の `Race`/`Subrace` の JSON に新フィールドを追加し、同じ `json.gz` に同梱してFTPアップロードする(解説専用の別ファイルは作らない)
- **文面の出し分け**: 本リポジトリでは最終結果の分析は行わず、常に「途中経過」の文面のみを生成する。レース終了後の最終結果表示はクライアント側で(このデータを使わずに)対応するため、本パイプラインの解説文生成はスコープ外とする
- **文体**: 解説風([summary.md](summary.md) のサンプル要約文の文体)に確定
- **分析に使用するAI**: OpenAI APIを使用する
- **ボーダー情報の管理**: 「1位」「10位」「100位」など、注目すべきボーダー順位はレースによって異なる。[border.md](border.md) はその一覧を人間が参照するための設計ドキュメントとし、実装時は同じ内容を既存の `RaceConfig` enum(`borderRanks` フィールドを追加する想定)に転記して保持する。border.mdを実行時にパースすることはしない(既存の `RaceConfig`/`NameIdentifierConfig` と同じ「設定はenumで持つ」パターンに合わせる)
- **プロンプトに含めるデータ量**: 生のスナップショット全件(最大1000位分のランクリスト)をそのまま渡すことはしないが、Java側で4分析軸(ボーダー推移/順位変動/勢い/新規・圏外)を計算して渡す事前集計方式は、事前処理の基準設定が複雑になるため**採用しない(破棄)**。代わりに、直近5スナップショットについて、各スナップショットから「1〜15位」と「各ボーダーの前後10位」(1-15位に含まれるボーダーは対象外)の範囲のみを抽出した、ほぼ生の順位データ(rank/point/name/id)をプロンプトに渡す。順位変動・勢い・新規ランクイン/圏外落ちといった分析自体はJavaで計算せず、OpenAI側に行わせる(下記「入力トークン量の見積もり」参照)
- **スナップショットが5件未満の場合(レース序盤)**: 集計・OpenAI呼び出しをスキップせず、その時点で存在するだけの件数で集計・生成する(5件は「最低限確保したい件数」であって「下回ったら生成しない」下限ではない)
- **障害時の挙動**: OpenAI呼び出しが失敗した場合、解説フィールドは空にし、エラー原因を別フィールド(エラー原因フィールド)に記載する。処理全体は失敗させず、既存の順位データのアップロードは通常通り継続する

## 分析エンジン(OpenAI API)について

`C:\work\aiinfobot`(Discord向けAIニュース要約Bot)の実装を参考に、以下の方針を踏襲する。

### 踏襲するパターン

- **APIキー管理**: `OPENAI_API_KEY` を平文の環境変数として渡す。Secrets Manager/Parameter Storeは使用しない。本リポジトリは既に `FC2_FTP_PASSWORD` を同じ方式(環境変数)で扱っており、方針と一貫する
- **モデル指定**: 環境変数(例: `OPENAI_MODEL`)でモデル名を上書き可能にし、未設定時はコード内のデフォルト値を使う。具体的なモデル名は未定(下記「未確定事項」参照)。ただし本プロジェクトはaiinfobotと異なり、順位変動・勢い・新規/圏外の**判定・計算自体をOpenAI側に行わせる**ため、単純な要約以上の推論能力が必要になる点に注意(詳細は下記「入力トークン量の見積もり」参照)
- **プロンプト設計**: システムプロンプトに分析観点(本設計書の「提案した分析軸」に相当。順位変動・ボーダー攻防・勢い・新規/圏外の判定方法を明文化)と対象レースのボーダー順位を含め、ユーザーメッセージとして直近5スナップショット分の順位データ(1-15位 + 各ボーダー前後10位に絞ったもの)をJSONで渡す。出力は前置き無しの構造化JSON形式のみを返させ(`response_format: {"type": "json_object"}` 相当)、temperatureは低め(aiinfobotでは0.3)にしてブレを抑える
- **障害時の設計思想**: aiinfobotは収集元の一部失敗を許容し、部分的な結果でもレポート送信を続行する設計(処理全体を止めない)。本プロジェクトでも同様に、解説文生成(OpenAI呼び出し)が失敗しても既存の順位データのアップロードという主機能は継続させる。具体的には、解説フィールドを空にし、エラー原因を別フィールドに記載した上で処理を続行する(上記「確定した要件」参照)

### 本プロジェクト固有の技術的差異

- aiinfobotはPython(Lambda ランタイム3.13)で公式`openai`パッケージを使用しているが、本プロジェクトはJava(`sourceCompatibility 1.8`)であり、`openai`パッケージは使えない。OpenAI REST APIを直接HTTP呼び出しする実装が必要
- Java 11以降の`java.net.http.HttpClient`は`sourceCompatibility 1.8`のため使用不可。AWS SDK(`aws-java-sdk-s3`)が既にApache HttpClientを推移的依存として持ち込んでいる(`RankConverterMain`内で`org.apache.http.client.protocol.RequestAcceptEncoding`をimport済み)ため、新規ライブラリを追加せずApache HttpClientでOpenAI APIを呼び出せる見込み
- レスポンスのJSONパースは既存の`jackson-databind`がそのまま使える

## Javaクラス構成

前節の通り、順位変動・勢い・新規/圏外といった**分析(判定・計算)はJavaでは行わず、OpenAI側に行わせる**方針に変更した。そのためJava側の役割は「分析」ではなく「プロンプトに含めるデータの抽出(選別)」に縮小される。

- **選別と送信を分離する**: プロンプト用データを選ぶクラスと、OpenAI APIへ送信するクラスを分ける
  - 例: `PromptSnapshotSelector`(仮称) — `Subrace` + `RaceConfig`(ボーダー順位`borderRanks`を含む)を受け取り、直近5スナップショットそれぞれについて「1〜15位」+「各ボーダー(1-15位に含まれるものを除く)の前後10位」に該当する`RankItem`のみを抽出したデータ(例: `PromptPayload`)を返す。順位変動・勢い等の計算は行わない、単純なフィルタリング処理。既存の`NameIdentifier`/`S3Downloader`と同様、パイプラインの1ステップとして`RankConverterMain.execute()`から呼び出す
  - 例: `OpenAiClient`(仮称) — `PromptPayload`とシステムプロンプト(分析観点 + 対象レースのボーダー順位)を受け取り、Apache HttpClientでOpenAI APIを呼び出し、解説文(または失敗時のエラー原因)を返す
- **選別結果はJSONでそのままプロンプトに渡す**: `PromptPayload`はJacksonの既存`ObjectMapper`でJSON化し、ユーザーメッセージとして渡す(手組みの日本語テキストにはしない)。実データから作成したサンプルJSONは [prompt-data-sample.md](prompt-data-sample.md) を参照
- 分析軸ごとにクラスを分割する設計は不要になった(分析自体がOpenAI側の責務のため)

## 入力トークン量の見積もり・モデル選定

### 見積もり方法

`sample/slimerace7.json` の実データ(1件あたり平均52.4文字のコンパクトJSON、例: `{"rank":100,"point":74710000,"name":"（ないしょ）","id":200}`)を基準に、3つの設計案を比較した。

| 設計案 | 内容 | 5スナップショット分の推定データ量 | 概算トークン数 |
|---|---|---|---|
| A. 生データをそのまま渡す(不採用) | 1000位分のランクリスト全件 × 5スナップショット = 5,000件 | 約262,200文字 | 約87,000〜131,000トークン |
| B. Java側で4分析軸を事前集計する(不採用・破棄) | ボーダー得点推移 + 順位変動・勢い・新規/圏外の集計結果 | 数千文字程度 | 概ね1,000〜2,000トークン |
| C. 1-15位 + ボーダー前後10位を抽出して渡す(採用) | slimerace/casinoraidなど(ボーダー1位/10位/100位): 26件/スナップショット。fishing/daifugo/daifugom/pencil(ボーダーが100位・200位・1000位を含む): 47件/スナップショット | 約7,100文字(slimerace実測) 〜 約12,300文字(fishing/daifugo系、推定) | 約2,400〜6,200トークン |

(設計案Cの47件/スナップショットの内訳: 1-15位で15件 + 100位ボーダー前後10位で21件 + 200位/1000位ボーダー前後10位で最大21件だが、追跡順位数の上限でクランプされ実際は11件程度)

### 結論

- 設計案B(Java側で4分析軸を事前集計する方式)は、事前処理の基準(オーバーテイクの判定範囲、勢いの計算対象など)の設計・実装コストが大きいため、**破棄**した
- 採用した設計案Cは、追跡順位数が最大(1000位)のレースであっても、プロンプトに含めるデータ量は概算で6,200トークン程度に収まり、**入力トークン上限は引き続き問題にならない**(miniクラスのモデルの入力上限、目安128,000トークン程度に対して十分な余裕がある)
- ただし設計案Cでは、順位変動・勢い・新規ランクイン/圏外落ちの**判定・計算をOpenAI自身が行う**必要があり、設計案B(Java側で計算済みの結果を渡すだけ)と比べて要求される推論の負荷が上がる。具体的には、最大47件×5スナップショット=235件程度の構造化データから、得点差分の計算、順位変動の検出、直近スナップショットに存在しないIDの検出(新規/圏外)を自力で行う必要がある
- **モデル選定への影響**: 入力トークン上限は制約にならないため、コスト面だけを見れば軽量(mini/nano)クラスのモデルでも問題ないが、上記の推論負荷を踏まえると、算数・数値比較の精度が重要になる。軽量モデルほど桁数の大きい得点(例: 600,000,000点)の差分計算や、複数IDにまたがる入れ替わり検出でのケアレスミスが起きやすい
- **結論(モデル提案の見直し)**: 今回のデータ量(数千トークン)であれば、上位モデル(mini未満に節約する意味がない)を使ってもコスト差はごくわずかである。そのため、**節約より精度を優先し、miniクラスより上位の標準/フラッグシップクラスのモデルを第一候補とする**ことを提案する。コストが問題になった場合にのみ、miniクラスへの切り替えを検討する(`OPENAI_MODEL`環境変数で切り替え可能な設計のため、後から変更しやすい)
- 前提条件: 上記の私(Claude)の知識は2026年1月時点までのため、2026年9月時点でのOpenAIの現行モデルのラインナップ・価格は変わっている可能性がある。実装時に現行モデル一覧を確認し、可能であれば実データで数値の正確性(得点差分・順位変動・新規/圏外の検出漏れがないか)を検証した上でモデルを確定すること

## 提案した分析軸

1. **順位変動・攻防** — 直近スナップショット間のオーバーテイク検出、首位争いの点差推移
2. **ボーダーライン攻防** — レースごとに複数存在するボーダー([border.md](border.md) 参照。例: 1位/10位/100位)それぞれについて、得点推移・僅差争い・保持者の入れ替わり頻度を分析する
3. **勢い・ペース分析** — 単位時間あたりの得点増加率(時給換算)による急上昇/失速判定、終盤の着地予想
4. **新規ランクイン/圏外落ち** — 新規表示・消失したプレイヤーの検出、匿名(（ないしょ）)プレイヤーは名前を伏せたまま動向のみ言及

以下は検討したが不要と判断し、対象外とした:
- ~~記録・マイルストーン(そのラウンドの最高得点更新、得点上限到達などの節目)~~
- ~~レース種目特性に応じた切り口(カジノレイド4種目やトリニティ6サブレースの横断比較など)~~

## 実データによる検証・サンプル要約文

実データ(スライムレース第7回、[sample/slimerace7.json](sample/slimerace7.json))を用いた検証結果とサンプル要約文は [summary.md](summary.md) を参照。

## 未確定・要検討事項

- **OpenAIモデルの確定値**: 上記「入力トークン量の見積もり・モデル選定」により、入力上限は問題にならず、判定・計算の精度を優先してmini未満ではなく標準/フラッグシップクラスのモデルを軸に検討する方針とした。具体的なモデル名は未定
- **プロンプトの具体的な文面**: システムプロンプトに組み込む分析軸の詳細な記述(順位変動・勢い・新規/圏外の判定方法をOpenAIにどう指示するか)、出力JSONのスキーマ(解説文のみか、判定に使った根拠データも含めるか)
- **OpenAI呼び出しのJava実装方法**: Apache HttpClientで直接REST呼び出しする案を想定しているが、他に適したライブラリがあれば要検討
- **エラー原因フィールドの形式**: OpenAI呼び出し失敗時に記録する「エラー原因」の粒度(例外メッセージそのままか、種別を分類するか)、DTO上のフィールド名・型
- **trinity(バトルトリニティ)のボーダー情報**: 参照元の `rankanalytics.js` に定義が見つからなかったため [border.md](border.md) 上で未確認。別途調査して補完する必要がある
